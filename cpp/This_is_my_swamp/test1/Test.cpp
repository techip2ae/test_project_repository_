#include <iostream>

using namespace std;
 
int main()
{
    for (int i=0 ; i==0 ; ){
    int s,u;
    cout << ("write the number: "), cin >> (s);
    cout << ("write another number , but bigger: "), cin >> (u);
        if (s < 0 , u < 0){
            i = 1;
        while ((s-1) < u){
            cout << ("numbers....cool = ") << s << endl;
            s=s-1;
            s=0;
           } 
 }
 else            
        if (s != u){
            i = 1;
        while ((s-1) < u){
            cout << ("numbers....cool = ") << s << endl;
            ++s;           
        }
        s = 0;
        } 
    }
}


    